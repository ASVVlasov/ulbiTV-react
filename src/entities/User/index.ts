import { userReducer } from 'entities/User/model/slice/userSlice';
import { User } from './ui/User';
import { type IUser, type IUserSchema } from './model/types/user';

export { userReducer, User, type IUser, type IUserSchema };
